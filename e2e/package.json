{
  "name": "shalom-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright e2e suite. POMs under pages/, specs under tests/, fixtures under fixtures/. POM/selector infrastructure originally generated with `ppg workspace ./frontend -o ./e2e` and hand-curated. http-server is a devDep because scripts/Start-FreshStack.ps1 resolves it from e2e/node_modules.",
  "scripts": {
    "pretest": "node scripts/prepare.mjs",
    "test": "playwright test --config=playwright.config.ts",
    "prepare:db": "node scripts/prepare.mjs",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^25.9.3",
    "http-server": "^14.1.1",
    "typescript": "^5.5.0"
  }
}
