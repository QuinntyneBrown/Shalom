// Production origin not provisioned yet — replace when the Shalom API is
// deployed. Keeping localhost makes a production build runnable against a
// local stack in the meantime.
export const environment = {
  apiBaseUrl: 'http://localhost:5100',
};
