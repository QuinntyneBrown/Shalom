/*
 * Public API Surface of components
 *
 * Shalom UI atoms. Each component's Angular `selector` keeps the
 * `sh-*` namespace; the exported TypeScript identifier drops the `Sh`
 * prefix since the import path already says where it came from.
 */

export * from './lib/bottom-nav/bottom-nav';
export * from './lib/pillar-card/pillar-card';
